# Copy to .env at the repository root and fill in two distinct random values.
# Do not commit .env or use production credentials for local/CI testing.
RESERVATION_SERVICE_SECRET=
RENTAL_SERVICE_SECRET=
