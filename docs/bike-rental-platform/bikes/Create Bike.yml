info:
  name: Create Bike
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_URL}}/bikes"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
