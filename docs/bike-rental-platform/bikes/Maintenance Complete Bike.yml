info:
  name: Maintenance Complete Bike
  type: http
  seq: 6

http:
  method: POST
  url: "{{BASE_URL}}/<bikeId>/maintenance/complete"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
